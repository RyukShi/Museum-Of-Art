<form method="POST"
    action="{{ path('app_classification_delete', {'id': classification.id}) }}"
    onsubmit="return confirm('Are you sure you want to delete this classification?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classification.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
